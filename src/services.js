import React from 'react'
import './Home.css'

function services() {
  return (
    <div className='Nav-gk'>
      <h1>Routing in React</h1>
      <p>
In a single-page React Application, routing refers to the process of navigating between different pages without triggering a full page reload. The application initially loads a single HTML page. Then, it dynamically renders different components based on user interaction.</p>
    </div>
  )
}

export default services
